Fix Order History & Admin Orders PagesGoal: Fix the 500 error on the frontend order history page, resolve the PROFILE NOT FOUND error in the admin panel, and ensure a secure and functional experience for both unauthenticated and authenticated users.the previous attempts to fix the order history page failed because we were addressing the wrong bugs. We now have a clearer understanding of the root causes. Your task is to provide a comprehensive and safe plan to fix both the frontend and backend order page issues, and present it for my review and approval.ACTION: Fix Order History & Admin Orders PagesPart 1: Fix Frontend Order History Page (/api/orders/my-orders)Diagnose the new 500 Internal Server Error on the /api/orders/my-orders endpoint. It is likely a bug in the backend code that retrieves the orders.Propose a solution to safely handle orders for unauthenticated users. Since you allow them to check out, you must provide a way for them to view their purchases. The best way to do this is to display a link on the "Payment Successful" page that contains a unique, temporary token to view the order details. This prevents the need for unauthenticated users to log in to see their orders.Propose the files to be modified in the backend and frontend to implement this solution.Part 2: Fix Admin Panel Orders Page (/admin/orders)Diagnose the PROFILE NOT FOUND error. The "Orders" button in the admin panel is likely linked to the wrong route.Propose a solution to fix the routing. The "Orders" button should navigate to a dedicated admin orders page (e.g., /admin/orders) that displays a list of all orders, not a profile-specific page.Propose the files to be modified in the frontend and backend to implement this solution.General Strategy & Safeguards:Detail how you will handle both bugs separately and ensure the fix for one does not break the other.Specify your testing strategy to confirm that both the frontend and backend order pages are working correctly.Present your complete approach and strategy, identifying specific file paths and detailing the proposed changes, for my review and approval before any code is modified.